# Troubleshooting

## Kivy does not start

Install dependencies inside `C:\mavi_project\.venv` and avoid global packages.

## Server cannot write database

Confirm the process can write to `C:\mavi_project\mavi.sqlite3`.

## Android build fails

Run Buildozer from Linux or WSL and install Android SDK/NDK prerequisites.
