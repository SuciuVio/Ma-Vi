# Server Setup

Run the server locally:

```bash
python -m server.server
```

Configuration is read from `MAVI_HOST`, `MAVI_PORT`, `MAVI_DB`, `MAVI_TOKEN_TTL`, `MAVI_REFRESH_TTL`, and `MAVI_LOG_LEVEL`.
